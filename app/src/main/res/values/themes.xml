<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 使用最基础的 AppCompat 主题，确保编译通过 -->
    <style name="Theme.MaimaiAccountInfo" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- 这里可以定义一些基础窗口属性 -->
        <item name="android:windowBackground">@android:color/white</item>
        <!-- 注意：这里不要定义任何 Material 3 相关的颜色属性（如 colorPrimary） -->
    </style>
</resources>